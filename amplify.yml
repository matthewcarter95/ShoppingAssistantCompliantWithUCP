version: 1
frontend:
  phases:
    build:
      commands:
        - echo "No build needed for vanilla JS frontend"
  artifacts:
    baseDirectory: /frontend
    files:
      - '**/*'
  cache:
    paths: []
